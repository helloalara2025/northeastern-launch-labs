/**
 * Common component barrel exports.
 *
 * Reusable UI components shared across multiple pages.
 * These are not page-specific and not shadcn/ui primitives.
 */
export { default as FloatingContact } from "./FloatingContact";
export { default as InstagramFeed } from "./InstagramFeed";
export { default as ErrorBoundary } from "./ErrorBoundary";
export { ManusDialog } from "./ManusDialog";
