# Todo

- [x] Fix nested `<a>` tag error in `Events.tsx`.
- [x] Simplify the copy on the Leadership page (`Leadership.tsx`) while keeping the names and roles intact.
- [ ] Simplify the main headers in `LaunchTeams.tsx`.
- [ ] Simplify the main headers in `InnovationTeams.tsx`.
- [ ] Simplify the main headers in `Leadership.tsx`.
